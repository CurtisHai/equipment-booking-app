from django.apps import AppConfig

class BookingsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    
    # Specifies the name of the app as 'bookings'
    name = 'bookings'
